apiVersion: v1
kind: Service
metadata:
  name: {{ include "shikha.fullname" . }}
  labels:
    {{- include "shikha.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shikha.selectorLabels" . | nindent 4 }}
