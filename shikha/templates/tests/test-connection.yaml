apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shikha.fullname" . }}-test-connection"
  labels:
    {{- include "shikha.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "shikha.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
